1) KiCad file backup consists of automatic, compressed .zip files (found in a backups subfolder).
2) The fp-info-cache in KiCad is a transitory cache file used to speed up the loading and viewing of footprint libraries. It is automatically regenerated by KiCad if deleted or not found, so it is not necessary to commit it to a Git repository